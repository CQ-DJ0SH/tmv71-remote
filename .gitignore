# Python
backend/.venv/
__pycache__/
*.py[cod]
*.egg-info/
.env

# Runtime / logs
*.log
*.wav
backend/app/runtime.json
backend/app/logbook.json
# voice profiles (biometric data — stays on the Pi, never committed)
backend/app/speakers.json

# Vosk speech model for callsign recognition (downloaded on setup, ~90 MB) +
# the extracted BNetzA callsign-list cache (models/rufzeichenliste.txt)
models/

# Official BNetzA callsign list PDF — supplied on the Pi, never committed
rufzeichenliste_afu.pdf

# Title-bar logo (may be a trademarked brand logo — never commit it)
backend/app/branding/

# TLS certs
backend/certs/

# Downloadable root CA (public cert; offered for phone install)
frontend/tm-v71-ca.crt

# Vollständiger Quellcode-Snapshot (lokales Backup, nie committen)
PROJECT-SNAPSHOT.md
